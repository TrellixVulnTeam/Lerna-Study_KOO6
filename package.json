{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "lerna run test --scope=@lerna-study/add   ",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
